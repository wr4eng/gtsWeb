<?php

/**
 * This is the model base class for the table "Diagnostic".
 * DO NOT MODIFY THIS FILE! It is automatically generated by AweCrud.
 * If any changes are necessary, you must set or override the required
 * property or method in class "Diagnostic".
 *
 * Columns in table "Diagnostic" available as properties of the model,
 * and there are no model relations.
 *
 * @property string $accountID
 * @property string $deviceID
 * @property integer $isError
 * @property string $codeKey
 * @property string $timestamp
 * @property string $binaryValue
 *
 */
abstract class BaseDiagnostic extends AweActiveRecord {

    public static function model($className=__CLASS__) {
        return parent::model($className);
    }

    public function tableName() {
        return 'Diagnostic';
    }

    public static function representingColumn() {
        return 'accountID';
    }

    public function rules() {
        return array(
            array('accountID, deviceID, isError, codeKey', 'required'),
            array('isError', 'numerical', 'integerOnly'=>true),
            array('accountID, deviceID', 'length', 'max'=>32),
            array('codeKey, timestamp', 'length', 'max'=>10),
            array('binaryValue', 'safe'),
            array('binaryValue', 'default', 'setOnEmpty' => true, 'value' => null),
            array('accountID, deviceID, isError, codeKey, timestamp, binaryValue', 'safe', 'on'=>'search'),
        );
    }

    public function relations() {
        return array(
        );
    }

    /**
     * @return array customized attribute labels (name=>label)
     */
    public function attributeLabels() {
        return array(
                'accountID' => Yii::t('app', 'Account'),
                'deviceID' => Yii::t('app', 'Device'),
                'isError' => Yii::t('app', 'Is Error'),
                'codeKey' => Yii::t('app', 'Code Key'),
                'timestamp' => Yii::t('app', 'Timestamp'),
                'binaryValue' => Yii::t('app', 'Binary Value'),
        );
    }

    public function search() {
        $criteria = new CDbCriteria;

        $criteria->compare('accountID', $this->accountID, true);
        $criteria->compare('deviceID', $this->deviceID, true);
        $criteria->compare('isError', $this->isError);
        $criteria->compare('codeKey', $this->codeKey, true);
        $criteria->compare('timestamp', $this->timestamp, true);
        $criteria->compare('binaryValue', $this->binaryValue, true);

        return new CActiveDataProvider($this, array(
            'criteria' => $criteria,
        ));
    }

    public function behaviors() {
        return array_merge(array(
            'CTimestampBehavior' => array(
                'class' => 'zii.behaviors.CTimestampBehavior',
                'createAttribute' => null,
                'updateAttribute' => 'timestamp',
                'timestampExpression' => new CDbExpression('NOW()'),
            )
        ), parent::behaviors());
    }
}